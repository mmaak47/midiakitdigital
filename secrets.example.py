# secrets.example.py — TEMPLATE ONLY. Copy to secrets.py and fill in real values.
# secrets.py is gitignored and must NEVER be committed.

# Current production VPS
VPS_HOST = 'YOUR_VPS_IP'
VPS_USER = 'root'
VPS_PASS = 'YOUR_VPS_ROOT_PASSWORD'

# Old / decommissioned VPS (Azure)
OLD_VPS_HOST = 'OLD_VPS_IP'
OLD_VPS_USER = 'mmak'
OLD_VPS_PASS = 'OLD_VPS_PASSWORD'
